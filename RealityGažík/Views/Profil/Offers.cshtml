<link rel="stylesheet" href="/css/tables.css">
<link rel="stylesheet" href="/css/changeButton.css">
<partial name="_profile"></partial>

@if (TempData["Message"] != null)
{
    <div class="change">@TempData["Message"]</div>
}

<table>
    <thead>
        <tr>
            <th>Username</th>
            <th>Name</th>
            <th>Email</th>
            <th>Tel</th>
            <th>Remove</th>
            <th>Promote</th>
            <th>Demote</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Offer item in ViewBag.Offers)
        {
            <tr>
                <td>@item.name</td>
                <td>@item.price</td>
                <td>@item.location</td>
                <td>@item.description.Substring(0, Math.Min(20, item.description.Length))</td>
                <td class="brokerIcon">
                    <a asp-controller="profil" asp-action="brokerRemove" asp-route-idBroker="@item.id">
                        <i class="bi bi-trash brokerIcon"></i>
                    </a>
                </td>
                <td class="brokerIcon">
                    <a asp-controller="profil" asp-action="brokerPromote" asp-route-idBroker="@item.id">
                        <i class="bi bi-award brokerIcon"></i>
                    </a>
                </td>
                <td class="brokerIcon">
                    <a asp-controller="profil" asp-action="brokerDemote" asp-route-idBroker="@item.id">
                        <i class="bi bi-arrow-down-circle brokerIcon"></i>
                    </a>
                </td>
            </tr>

        }
    </tbody>

</table>

